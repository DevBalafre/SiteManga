{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{asset('assets/css/homepage.css')}}">
{% endblock %}



{% block title %}Hello UserController!{% endblock %}

{% block body %}

{#
Classer les manga sur la page principale via les derniers chapitres

Faire un CRUD dans l'espace gestion de compte #}




<table class="table">
    {# <tbody>

        Affichez les Categorie existante losque l'on clique sur toutes les catégories

        {% for categorie in listCategorie %}
        <tr>
            <td class="d-flex justify-content-between"> <strong>{{ categorie.title }}</strong> </a></td>
        </tr>
        {% endfor %}
    </tbody> #}
</table>



<div class="parent">
    {% for manga in lastManga %}


    <div class="manga">
        <a class="img" href="{{ path('app_manga', {'id': manga.id}) }}">
            <img src="{{ asset('uploads/imageManga/' ~ manga.photoManga) }}" alt="">
        </a>

        <div class="second">

            <a href="{{ path('app_manga', {'id': manga.id}) }}">
                <h3>{{ manga.title }}</h3>
            </a>

            <div class="chapterList">

                {# Boucle for avec des a pour chaque chapitre #}

                {% for chapter in manga.chapters|reverse %}

                {% if loop.index <= 4 %} <a href="#">{{ chapter.title}} </a>

                    {% endif %}

                    {% endfor %}

            </div>

        </div>



        

    </div>

    {% endfor %}

</div>
{# Faire la Pagination #}
<a href="">Pagination</a>


{% endblock %}